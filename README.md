# Graph Traversal

This is a simple program made in C++ to traverse a graph using 2 methods, depth-first-search, which utilizes stacks and breath-first-search which utilizes queues. In this sense we managed to build a nice way to optimally traverse graphs where the graph was an adjacency matrix.
